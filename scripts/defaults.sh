#!/usr/bin/env bash
# Validated defaults. Source after scripts/load-env.sh.
set -euo pipefail
CONTAINER_NAME="${CONTAINER_NAME:-glm53-flash-2x-rtxpro6000}"
SERVED_MODEL_NAME="${SERVED_MODEL_NAME:-glm-5.3-flash-local}"
RUNTIME_IMAGE="${RUNTIME_IMAGE:-ghcr.io/tpurtell/glm-5.3-flash-exl3-4bpw-2x-rtx:v0.6.0@sha256:fe249b88d091430d8a88cd987d087d556053f0f067a649f2e9ca95895129e82b}"
BIND_ADDRESS="${BIND_ADDRESS:-127.0.0.1}"
PORT="${PORT:-8000}"
GPU_DEVICES="${GPU_DEVICES:-0,1}"
CACHE_DIR="${CACHE_DIR:-${ROOT}/.cache/vllm}"
MAX_MODEL_LEN="${MAX_MODEL_LEN:-1048576}"
MAX_NUM_BATCHED_TOKENS="${MAX_NUM_BATCHED_TOKENS:-2048}"
MAX_NUM_SEQS="${MAX_NUM_SEQS:-16}"
GPU_MEMORY_UTILIZATION="${GPU_MEMORY_UTILIZATION:-0.950}"
KV_CACHE_DTYPE="${KV_CACHE_DTYPE:-fp8_ds_mla}"
MAX_IMAGES_PER_PROMPT="${MAX_IMAGES_PER_PROMPT:-16}"
MAX_VIDEOS_PER_PROMPT="${MAX_VIDEOS_PER_PROMPT:-0}"
DFLASH_TOKENS="${DFLASH_TOKENS:-5}"
DFLASH_KV_CACHE_DTYPE="${DFLASH_KV_CACHE_DTYPE:-bfloat16}"
ACCEPT_DFLASH2_RESEARCH_LICENSE="${ACCEPT_DFLASH2_RESEARCH_LICENSE:-0}"
ENABLE_EXPERT_PARALLEL="${ENABLE_EXPERT_PARALLEL:-1}"
DECODE_CONTEXT_PARALLEL_SIZE="${DECODE_CONTEXT_PARALLEL_SIZE:-2}"
ENABLE_PREFIX_CACHING="${ENABLE_PREFIX_CACHING:-1}"
NCCL_DEBUG="${NCCL_DEBUG:-WARN}"
